import React from 'react';

const AppSidebar = () => {
    return (
        <div class="sidebar">
            <a class="active" href="">Dashboard</a>
            <a href="">Profile</a>
            <a href="">Contact</a>
        </div>
    );
};

export default AppSidebar;
